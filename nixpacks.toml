[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "glib", "cairo", "pango"]

[phases.install]
cmds = [
  "python3 -m venv venv",
  "venv/bin/pip install --upgrade pip",
  "venv/bin/pip install -r requirements.txt"
]

[start]
cmd = "venv/bin/python3 main.py"
